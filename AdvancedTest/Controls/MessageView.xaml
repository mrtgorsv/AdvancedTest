<UserControl x:Class="AdvancedTest.Controls.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel.Background>
                <ImageBrush ImageSource="{Binding Image}"></ImageBrush>
            </StackPanel.Background>
            <TextBlock x:Name="label" Text="{Binding Message}" FontSize="24" FontStyle="Oblique" Foreground="{Binding Path=TextColor}"
                       TextWrapping="Wrap" Margin="10"/>
        </StackPanel>

    </Grid>
</UserControl>
