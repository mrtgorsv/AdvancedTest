<Window x:Class="AdvancedTest.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdvancedTest.Views"
        mc:Ignorable="d"
        Title="Вход в систему" Height="300" Width="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Label x:Name="label" Content="Логин"/>
            <TextBox x:Name="textBox" Height="22" TextWrapping="Wrap" Text="{Binding Login}" />
            <Label x:Name="label1" Content="Пароль"/>
            <TextBox x:Name="Password" Height="23" TextWrapping="Wrap" Text="{Binding Password}"/>
            <Button x:Name="LoginButton" Content="Вход" Command="{Binding LoginCommand}"/>
            <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="{Binding ErrorText}" Height="132"/>

        </StackPanel>
    </Grid>
</Window>
